spring:
  application:
    name: receipt-extractor
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

ocr:
  tessdata-path: src/main/resources/tessdata
  languages: vie+eng

sheets:
  credentials-path: config/sheets-credentials.properties
